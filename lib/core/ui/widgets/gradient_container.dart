import 'package:bloc_practise/core/themes/app_pallet.dart';
import 'package:flutter/material.dart';

final Container gradientContainer = Container(
  decoration: const BoxDecoration(
      gradient: LinearGradient(colors: [
    // blackColor,
    // whiteColor,
    blueColorg1,

    blueColor,
    blueColorg1,
  ])),
);
